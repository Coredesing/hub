import CircularProgressMui from '@material-ui/core/CircularProgress'

const CircularProgress = (props: any) => {
    return <CircularProgressMui style={{ color: '#fff', width: '28px', height: '28px' }} {...props} />
}

export default CircularProgress
