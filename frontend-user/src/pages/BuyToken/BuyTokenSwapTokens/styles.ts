import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme: any) => {
  return {
    sectionBuyTokenSwapTokens: {
      background: '#303035',
      borderRadius: 12,
      padding: '28px 28px',
      marginBottom: 12,
      color: '#FFFFFF',

      [theme?.breakpoints?.down('sm')]: {
        padding: '28px 20px',
      },
    },

    allowcationWrap: {

    },

    allowcationTitle: {

    },

    allocationContent: {
      
    }
  };
});

export default useStyles;